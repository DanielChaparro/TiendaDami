spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db?useUnicode=true&characterEncoding=utf8&createDatabaseIfNotExist=true
    database-platform: org.hibernate.dialect.MySQL5Dialect
    username: root
    password: 12345
  liquibase:
    enabled: false
  session:
    jdbc:
      initialize-schema: always
  jpa:
    hibernate:
      ddl-auto: create